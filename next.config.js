/** @type {import('next').NextConfig} */
const nextConfig = {
  // Ide jöhetnek a további érvényes Next.js konfigurációk,
  // pl. images, compiler, stb.
  // De az 'experimental' szekciót most üresen hagyjuk vagy töröljük.
};

module.exports = nextConfig;
